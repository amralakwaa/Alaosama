{
  "name": "orion-fullstack",
  "version": "1.0.0",
  "description": "Unified frontend and backend runner",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && cd .. && node copy-static.js",
    "start": "node server.js"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "jsonwebtoken": "^9.0.3",
    "node-fetch": "^3.3.2"
  }
}
